#inizalizzo una lista vuota
values = []

#si apre il file shampoo_sales.csv come lista  
lista = open('shampoo_sales.csv', 'r')
#per ogni linea della lista si dividono gli elementi
for line in lista:
  element = line.split(',')
  #si assegna all'elemento 0 il nome Date e all'elemento 1 il nome Value
  if element[0] != 'Date':
    Date=[0]
    value=[1]
def funzione_somma(value):
  for value in lista:
      int(value)
      value = value + 1
      return value
  print(value)
